<snippet>
    <content><![CDATA[
/**
 * Display the ${4:model} details.
 *
 * @param  integer \$id
 * @param  Response \$response
 * @return Response
 */
public function show(\$id, Response \$response)
{
    \$${1:model} = ${2:Model}::find(\$id);
    return \$this->view->render(\$response, "${3:twig-file}.twig", compact('${1:model}'));
}
]]></content>
    <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
    <tabTrigger>con-show</tabTrigger>
    <!-- Optional: Set a scope to limit where the snippet will trigger -->
    <scope>source.php</scope>
    <description>skeleton controller crud show</description>
</snippet>
